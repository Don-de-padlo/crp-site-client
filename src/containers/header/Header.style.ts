import type { SxProps, Theme } from "@mui/material";

const headerSx: SxProps<Theme> = {
    bgcolor: "black", 
    color: "white",
    width: "100%",
    top: 20,
    left: 0
}

export default headerSx